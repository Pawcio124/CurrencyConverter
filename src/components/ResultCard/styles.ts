import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
    card: {
        backgroundColor: 'rgba(134, 246, 134, 0.87)',
        padding: 15,
        borderRadius: 12,
    },
    result: {
        fontSize: 32,
    },
    resultDetails: {
        fontSize: 12,
    },
    currency: {
        textAlign: 'right',
    },
})
